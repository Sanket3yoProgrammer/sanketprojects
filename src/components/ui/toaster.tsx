import React from 'react';

export function Toaster() {
  // We're creating a simplified version that doesn't actually show toasts
  // This is just to prevent import errors after removing complex dependencies
  return <div id="toaster" className="hidden" />;
}
